name: Build iOS Project Package

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-ios-project:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Build iOS app for simulator
        run: |
          xcodebuild \
            -project BZGram.xcodeproj \
            -scheme BZGram \
            -configuration Release \
            -sdk iphonesimulator \
            -destination 'generic/platform=iOS Simulator' \
            CODE_SIGNING_ALLOWED=NO \
            build

      - name: Package Xcode project for local IPA build
        run: |
          VERSION="${{ github.run_number }}"
          mkdir -p dist
          zip -r "dist/BZGram-iOS-project-v${VERSION}.zip" \
            BZGram.xcodeproj \
            BZGram \
            Package.swift \
            README.md \
            project.yml

      - name: Publish release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION="${{ github.run_number }}"
          TAG="v${VERSION}"
          ASSET="dist/BZGram-iOS-project-v${VERSION}.zip"

          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" "$ASSET" --clobber
          else
            gh release create "$TAG" "$ASSET" \
              --title "iOS Project ${TAG}" \
              --notes "Automated Xcode project package for local IPA export from commit ${{ github.sha }}"
          fi
